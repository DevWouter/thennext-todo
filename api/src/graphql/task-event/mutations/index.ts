import { createTaskEvent } from "./create-task-event.mutation";
import { destroyTaskEvent } from "./destroy-task-event.mutation";

export const TaskEventMutations = {
    createTaskEvent,
    destroyTaskEvent,
};
