import { GraphContext } from "../../helpers";

export async function destroyChecklistItem(obj, args: { uuid: string }, context: GraphContext, info): Promise<boolean> {
    throw new Error("Not implemented");
}
