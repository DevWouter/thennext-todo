type ChecklistItem{
    uuid: Uuid!
    checked: Boolean!
    title: String!
    taskUuid: Uuid!
    task: Task!
}

input ChecklistItemInput {
    checked: Boolean
    title: String
}

type Mutation {
    createChecklistItem(taskUuid: Uuid!, input: ChecklistItemInput!): ChecklistItem
    updateChecklistItem(uuid: Uuid!, input: ChecklistItemInput!): ChecklistItem
    destroyChecklistItem(uuid: Uuid!): Boolean
}