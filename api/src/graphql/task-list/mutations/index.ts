import { createTaskList } from "./create-task-list.mutation";
import { updateTaskList } from "./update-task-list.mutation";
import { destroyTaskList } from "./destroy-task-list.mutation";

export const TaskListMutations = {
    createTaskList,
    updateTaskList,
    destroyTaskList,
};
