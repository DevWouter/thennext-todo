import { GraphContext } from "../../helpers";
import { Session } from "../session.model";

export async function sessions(obj, args, context: GraphContext, info): Promise<Session[]> {
    throw new Error("Not implemented");
}
