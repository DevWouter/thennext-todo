import { createChecklistItem } from "./create-checklist-item.mutation";
import { destroyChecklistItem } from "./destroy-checklist-item.mutation";
import { updateChecklistItem } from "./update-checklist-item.mutation";

export const ChecklistItemMutations = {
    createChecklistItem,
    destroyChecklistItem,
    updateChecklistItem,
};
